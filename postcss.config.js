/* 
 * This is the PostCSS configuration file for the Pronaflow frontend project.
 * It sets up the necessary plugins for processing CSS, including Tailwind CSS
 * for utility-first styling and Autoprefixer for adding vendor prefixes.
 */
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
- **Drag & Drop:** Đặc tả logic của `@dnd-kit` trong Kanban/Gantt (Module 4 & 5).
    
- **Real-time:** Cách xử lý WebSocket/Webhook cho hệ thống Notification (Module 7).
    
- **Personalization:** Cách lưu cấu hình người dùng (Theme, Compact mode) vào `localStorage` hoặc Entity `UserSettings`.
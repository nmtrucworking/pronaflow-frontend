- **Mô hình:** **Feature-based Architecture** (như đã thảo luận ở phần trước).
    
- **Data Flow:** * Server State: TanStack Query (React Query) hoặc Axios Services.
    
    - Client State: Zustand (cho Global State như Auth, Theme).
        
    - Form State: React Hook Form + Zod.